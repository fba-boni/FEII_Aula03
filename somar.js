/**
 * 1. Crie um arquivo somar.js contendo uma função chamada somar, que deve ser 
 * exportada no final do arquivo.Esta função deve receber 2 parâmetros e retornar a
 * soma dos mesmos.
 */

module.exports = function Somar(p1, p2) {
     return (p1 + p2);
}

// export function somar(p1, p2) {
//     return (p1 + p2);
// }
console.log(somar(5, 5));